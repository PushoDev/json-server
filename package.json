{
  "name": "api-rest.express",
  "version": "1.0.0",
  "description": "Pequeño Servidor de Información",
  "main": "app.js",
  "scripts": {
    "start": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pusho.Dev - Tech",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
